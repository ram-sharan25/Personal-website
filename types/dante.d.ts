declare module 'dante3' {
  // Define types for Dante components and functions
  // eslint-disable-next-line @typescript-eslint/no-empty-interface
  export interface Dante {
    // Define the props for DanteEditor component
    // Add properties according to the component's actual props
  }

  export class DanteEditor extends React.Component<DanteEditorProps> {
    // Define the component's methods, if applicable
  }
}
